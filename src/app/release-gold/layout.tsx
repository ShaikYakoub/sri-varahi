import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Release Pledged Gold - Sri Varahi | Fast Loan Closure",
  description:
    "Release your pledged gold by closing bank or NBFC loans with transparent valuation and instant balance payout from Sri Varahi.",
};

export default function ReleaseGoldLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
