import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Release Pledged Gold - Value Gold | Fast Loan Closure",
  description:
    "Release your pledged gold by closing bank or NBFC loans with transparent valuation and instant balance payout from Value Gold.",
};

export default function ReleaseGoldLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
